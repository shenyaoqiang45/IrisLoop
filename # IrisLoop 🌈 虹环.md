# IrisLoop 🌈 虹环

&gt; **AI 驱动的 MEMS 激光投影自适应闭环系统**
&gt;
&gt; *See the Light, Perfect the Light.*

IrisLoop 是一套将**原生多模态 AI** 与 **MEMS 激光投影**深度融合的交互式内容生成系统。系统通过 USB 摄像头实时采集物理表面的投影效果，利用 AI 视觉分析自主评估差距，并迭代优化投影内容，实现"生成 → 投射 → 观察 → 优化"的全自动闭环。

## ✨ 核心特性

- **视觉自主反馈**：AI 自己"长眼睛"，通过摄像头观察实际投影效果
- **原生多模态理解**：统一理解文本、图像、视频，避免语义漂移
- **MEMS 激光投影适配**：专有位图→MEMS 坐标流转换
- **物理表面自适应**：自动评估不同材质的投影表现并优化
- **数据飞轮**：历史迭代记录持续积累，形成自我进化

## 🎯 两个核心 AI 多模态待验证点

> 这是 IrisLoop 最有价值、也最想验证的两个能力。它们分别代表闭环的
> 「内容生成端」和「视觉理解端」，是整条链路的灵魂。

### 1️⃣ 提示词 → 视频生成 → 投射（内容生成端）

**目标**：验证大模型能否根据自然语言提示词直接生成 **3~5 秒短视频**，并适配成 MEMS 可投影的格式。

**链路**：
```
文本提示词 → 多模态大模型 → 生成 3~5s 视频
           → 拆帧 → 每帧 640×480 1bpp 二值化 → BLE 推送到素材组 → 播放
```

**关键挑战**：
- 生成视频的内容复杂度 vs MEMS 1bpp 单色显示的表达力鸿沟
- 帧率/时长与素材组上限（单组最多 20 帧）的折衷
- 视频转 1bpp 时的视觉保真（抖动算法、轮廓保留）

**验证判据**：投射出的视频内容是**可辨认的**（人眼能看出提示词描述的主体），且动画流畅。

### 2️⃣ 看懂投射的视频 → 优化内容（视觉理解端）

**目标**：验证多模态大模型能否**"看懂"摄像头拍到的实际投影视频**，评估**内容表达**的差距，并生成**优化后的新内容**。

> ⚠️ 设备出厂已校准好畸变/FOV/亮度等硬件参数，闭环**不调设备，只调内容**。

**链路**：
```
摄像头录下投射视频 → 多模态大模型视频理解
  → 评估内容表达（主体是否可辨认/轮廓是否清晰/对比度是否足够/动画是否连贯）
  → 直接生成优化后的新内容（重新二值化/增粗线条/简化细节/调整构图）
  → BLE 推送新内容 → 再拍 → 闭环收敛
```

**关键挑战**：
- 视频理解对 **1bpp 单色内容可辨识度**的判断（什么形状在激光投影下能看清）
- 模型生成的新内容能否**适配 MEMS 显示特性**（线条足够粗、细节足够少、对比足够强）
- 多轮迭代的内容演化方向是否收敛（而不是反复横跳）

**验证判据**：模型能从实拍视频中看出"这个图案投影后看不清/看不出是什么"，并生成一个**投影后明显更易辨认**的新版本，3 轮内内容质量明显提升。

---

## 🚀 快速开始

```bash
pip install -r requirements.txt
export MOONSHOT_API_KEY="sk-your-key"
python -m irisloop.analyzer